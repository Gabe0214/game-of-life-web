import { createMuiTheme } from '@material-ui/core/'

 const startTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#008000',
        }
        
    }
})


export default startTheme

